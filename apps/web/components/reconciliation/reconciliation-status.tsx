import { ReconciliationRunResponse } from "../../lib/api/reconciliation";

export function ReconciliationStatus({ latestRun }: { latestRun: ReconciliationRunResponse }) {
  // Logic to be implemented in metrics and history commit
  return <div>{/* Status Placeholder */}</div>;
}
